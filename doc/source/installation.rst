============
Installation
============

At the command line::

    $ pip install mercador-sub

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mercador-sub
    $ pip install mercador-sub
